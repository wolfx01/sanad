# دليل الشرح الشامل لمشروع «سند» (Sanad) لتقديمه للشركة

---

## 1. الفكرة العامة للمشروع (Executive Summary)
**منصة «سند»** هي منصة ذكية لأتمتة صياغة وتوليد العقود والوثائق القانونية (مثل عقد بيع سيارة مستعملة، عقد كراء شقة سكنية) باللغة العربية عبر **الذكاء الاصطناعي التفاعلي (Conversational AI)**.

### ما هي المشكلة التي يحلها المشروع؟
- كتابة العقود القانونية التقليدية تتطلب وقتاً ومحامياً أو كاتب عمومي، وكتابة النماذج يدوياً تؤدي لأخطاء نسيان بنود أساسية (رقم البطاقة، أرقام اللوحات، الشروط المالية).
- نماذج الويب التقليدية تكون عبارة عن استمارات طويلة وجافة يمل منها المستخدم.

### الحل الذي يقدمه «سند»:
- المستخدم يدخل في محادثة ذكية وودودة كأنه يتحدث مع محامٍ شخصي أو صديق.
- المساعد الذكي يطرح الأسئلة تدريجياً، يتحقق من المعطيات، وبمجرد اكتمالها يقوم النظام تلقائياً بتوليد وثيقة قانونية رسمية منسقة بدقة وجاهزة للتحميل كـ PDF والطباعة.

---

## 2. البنية التقنية للمشروع (Tech Stack)

| المكون | التقنية المستخدمة | الدور والأهمية |
| :--- | :--- | :--- |
| **الواجهة الأمامية (Frontend)** | Next.js 16 (React, TypeScript, App Router) | بناء واجهة شات حديثة وسلسة جداً مع ميزة العرض التفاعلي للوثائق. |
| **التصميم والأسلوب (Styling)** | Vanilla CSS خالص وعصري | تصميم هوية بصرية مخصصة ومظهر داكن/فاتح فاخر ومريح للمستخدم. |
| **قاعدة البيانات (Database)** | PostgreSQL (عبر Docker Container `pgvector-db`) | تخزين بيانات المستخدمين، جلسات المحادثة، القوالب القانونية، والوثائق المنتجة. |
| **محرك الذكاء الاصطناعي (AI)** | OpenRouter API (GPT-4o-mini) | فهم كلام المستخدم، استخراج الحقول القانونية بدقة، وتحويل الدردشة لبيانات منظمة. |
| **الأمان والمصادقة (Auth)** | JWT + Bcrypt | تشفير كلمات المرور وتأمين جلسات الدخول. |

---

## 3. كيف يعمل النظام من الداخل خطوة بخطوة؟ (Data Flow)

```
[المستخدم يكتب في الشات] 
        │
        ▼
[app/chat/page.tsx] ────► يرسل الرسالة عبر POST لـ [app/api/chat/route.ts]
                                      │
                                      ▼
                      [services/chat.service.ts] (العقل المدبر)
                                      │
        ┌─────────────────────────────┴─────────────────────────────┐
        ▼                                                           ▼
[قاعدة البيانات PostgreSQL]                                   [OpenRouter AI]
- استرجاع سجل الحوار السابق                             - تحليل الرسالة
- استرجاع حقول القالب القانوني المطلوبة                 - استخراج الحقول بصيغة JSON
                                                                    │
        ┌───────────────────────────────────────────────────────────┘
        ▼
هل اكتملت كل الحقول المطلوبة؟
  ├── [لا]: يطلب الحقل الناقص من المستخدم (مثال: "ما هو رقم تسجيل السيارة؟")
  └── [نعم]: 
        │
        ▼
  [services/document.service.ts]
  - تعويض المتغيرات في القالب بنجاح
  - حفظ المستند المولد في جدول generated_documents
  - إرسال رد نهائي للواجهة مع بطاقة تفاعلية جاهزة للطباعة [CONTRACT_ACTION:...]
```

---

## 4. شرح ملفات المشروع الأساسية (سطراً بسطر ودور كل ملف)

### أولاً: الخدمات الخلفية (Backend Services - مجلد `services/`)

#### 1. `services/chat.service.ts` (محرك إدارة الدردشة والذكاء الاصطناعي)
- **دوره:** هذا هو أهم ملف في المشروع بأكمله.
- **كيف يعمل؟:**
  - يحدد وظيفة المساعد (`System Prompt`): أنت مستشار قانوني ودود لمنصة سند، دورك جمع المعلومات المطلوبة فقط.
  - دالة `processUserMessage`:
    - تقرأ الرسائل القديمة من قاعدة البيانات حتى لا ينسى الروبوت سياق الكلام.
    - ترسل الرسالة إلى OpenRouter API وتطلب منه إرجاع كائن JSON يحتوي على الحقول المستخرجة وحالة الاكتمال (`is_complete`).
    - إذا اكتمل العقد (`is_complete = true`)، يستدعي `DocumentService` لتوليد العقد، ثم يضيف إلى الرد شفرة خاصة: `[CONTRACT_ACTION:preview:DOC_ID]` لكي تظهر للمستخدم أزرار التنزيل والمعاينة كبطاقة ثابتة.
    - عند انتهاء العقد وبدء المستخدم في طلب عقد جديد، يقوم الكود تلقائياً بإنشاء جلسة جديدة (`sessionId`) لمنع تداخل العقود مع بعضها وحفظ العقد القديم في السجل الجانبي.

#### 2. `services/document.service.ts` (محرك صياغة الوثائق)
- **دوره:** دمج الحقول التي جمعها الذكاء الاصطناعي مع القالب القانوني المعتمد.
- **دالة `renderDocument`:**
  - تأخذ نص القالب القانوني الذي يحتوي على متغيرات مثل: `{{seller_name}}` و `{{sale_price}}`.
  - تستبدل كل متغير بالقيمة التي تم تخزينها من إجابات المستخدم.
  - تحفظ الوثيقة الناتجة في قاعدة البيانات وتمنحها معرّفاً فريداً (`id`).

#### 3. `services/template.service.ts` (مكتبة القوالب القانونية)
- **دوره:** إدارة القوالب القانونية المعتمدة وتزويد قاعدة البيانات بالنماذج الجاهزة.
- **يحتوي على:**
  - قالب **عقد بيع سيارة مستعملة**: مع الحقول المطلوبة (اسم البائع، اسم المشتري، نوع وموديل السيارة، رقم اللوحة، الثمن، تاريخ التسليم).
  - قالب **عقد كراء سكني**: مع الحقول المطلوبة (المؤجر، المستأجر، عنوان العقار، السومة الكرائية، الضمانة، المدة، تاريخ البدء).

#### 4. `services/auth.service.ts` (نظام المستخدمين والأمان)
- **دوره:** إدارة عمليات التسجيل وتسجيل الدخول.
- يقوم بتشفير كلمة السر باستخدام مكتبة `bcryptjs` لحمايتها، ويولّد رمز مرور رقمي آمن `JWT` يُرسل في ملفات تعريف الارتباط (Cookies).

---

### ثانياً: واجهة المستخدم ومسارات الصفحات (مجلد `app/`)

#### 1. `app/chat/page.tsx` (واجهة الشات وتوليد العقود)
- **دوره:** الصفحة الرئيسية التفاعلية.
- **أهم مميزاتها البرمجية:**
  - **السجل الجانبي (Sidebar):** يستعرض العقود السابقة للمستخدم من قاعدة البيانات مع أوقاتها وحالاتها، وزر "عقد جديد" لبدء عقد آخر بكل مرونة.
  - **البطاقة التفاعلية التلقائية (`renderMessageContent`):** حين ينهي الذكاء الاصطناعي صياغة العقد، لا يظهر مجرد نص عادي، بل يتم تحويل الشفرة لبطاقة تصميمية بلون زمردي تمنح المستخدم زريْن: "معاينة العقد" و "طباعة / تحميل PDF".
  - **الطباعة النظيفة بنسبة 100% (`handlePrintCleanDocument`):**
    - ميزة نوعية: عندما يضغط المستخدم على زر الطباعة، تفتح نافذة مستقلة معزولة تماماً تقتصر على متن العقد القانوني الرسمي وخانات التوقيع («توقيع البائع» و «توقيع المشتري»)، بدون أي ظهور لعناصر الموقع أو القائمة الجانبية أو الأزرار.

#### 2. `app/api/chat/route.ts` (نقطة الاتصال API)
- **دوره:** نقطة النهاية (Endpoint) التي تستقبل طلبات المحادثة عبر بروتوكول HTTP POST، وتتحقق من صحة المستخدم ثم تحيل الطلب إلى `ChatService`.

---

## 5. دليل الإجابة على أسئلة لجنة الشركة (Cheatsheet للمقابلة والعرض)

### س1: لماذا استخدمتم نموذج ذكاء اصطناعي تفاعلي بدلاً من مجرد استمارة (Form)؟
> **الجواب النموذجي:**
> الاستمارات التقليدية غالباً ما تكون معقدة وتجعل المستخدمين العاديين يترددون أو يخطئون في إدخال المصطلحات القانونية. الشات الذكي يجعل التجربة إنسانية، ويسأل المستخدم بأسلوب سلس مثل المحامي، كما أنه يستطيع استنتاج عدة بيانات من جملة واحدة (مثلاً لو كتب المستخدم: "أنا كريم التازي أريد بيع سيارتي داسيا لأخي بلال بـ 8 ملايين"، فالذكاء الاصطناعي يستخرج اسم البائع والمشتري ونوع السيارة والثمن بذكاء من رسالة واحدة).

### س2: هل هناك خطر أن يخترع الذكاء الاصطناعي بنوداً غير قانونية (Hallucination)؟
> **الجواب النموذجي:**
> قمنا بحماية النظام من هذه المشكلة عبر معمارية هجينة (Hybrid Architecture):
> الذكاء الاصطناعي **لا يكتب** العقد من الصفر، بل يقتصر دوره على استخراج البيانات ومحاورة العميل، بينما تُحقن تلك البيانات داخل **قوالب قانونية محكمة ومغلقة ومصاغة مسبقاً وفق القانون المغربي**. وبالتالي نسبة الخطأ القانوني هي صفر.

### س3: كيف تم التعامل مع مسألة خصوصية وأمان بيانات العملاء؟
> **الجواب النموذجي:**
> - كلمات المرور مشفرة بخوارزمية التجزئة غير القابلة للعكس `bcrypt`.
> - جلسات الدخول مؤمنة عبر `JWT (HttpOnly cookies)`.
> - قاعدة بيانات PostgreSQL مفصولة ومحمية، وكل مستخدم لا يمكنه الاطلاع إلا على العقود الخاصة بحسابه حصراً عبر التحقق البرمجي على مستوى الخادم.

### س4: هل المشروع قابل للتوسع لإضافة عقود أخرى في المستقبل؟
> **الجواب النموذجي:**
> نعم بكل تأكيد؛ بنينا النظام بنظام معياري (Modular Design):
> لإضافة أي عقد جديد (مثل عقد عمل، عقد بيع عقار، إبراء ذمة)، يكفي فقط إضافة القالب وحقوله في مصفوفة القوالب في `template.service.ts` بدون الحاجة لتعديل كود الذكاء الاصطناعي أو واجهة الشات!
